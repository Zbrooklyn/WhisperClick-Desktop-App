# .planning/ — FROZEN GSD SNAPSHOT (state-machine refactor, COMPLETE)

> ⚠️ **This entire folder is a historical artifact. Do not plan current work from it.**
>
> It captures the GSD (Get-Stuff-Done) planning + codebase analysis for the
> **state-machine refactor**, which is **complete and shipped** (v2.2.0-beta, merged to
> `main`; the `feature/state-machine` branch is gone). Files here — `STATE.md`,
> `PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`, `codebase/CONCERNS.md`, `phases/` —
> describe that effort **as it was mid-flight**. Lines like "Phase 1 in progress" or
> "solution planned" reflect the snapshot date (Mar 2026), not today.
>
> **For current state:** `../HANDOFF.md` · **history:** `../SESSION-LOG.md` ·
> **the shipped refactor's design + post-mortem:** `../docs/dev/state-machine-refactor.md`
> and `../docs/dev/post-mortem-state-machine-refactor.md`.

_Banner added 2026-05-27 during continuity cleanup._
