# OpenAI API key (required for API mode only)
OPENAI_API_KEY=sk-your-key-here

# Whisper model for local mode: tiny, base, small, medium, large
WHISPER_MODEL=base

# Global hotkey to toggle recording
HOTKEY=<ctrl>+<shift>+r
