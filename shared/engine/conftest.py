"""Pytest config for the engine test suite.

Presence of this file at the engine root puts that directory on sys.path so
tests can `import backend.*` regardless of the working directory pytest is
invoked from.
"""
